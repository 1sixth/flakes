--embed-chapters

--extractor-args "youtube:skip=dash,hls,translated_subs"

--format-sort res:1080

--output "$PWD/%(title)s.%(ext)s"

--remux-video mkv

--sub-langs "en.*,zh.*"

--write-subs

--write-auto-subs
